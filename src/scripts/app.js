(() => {
  'use strict';

  var app = {
    isLoading: true,
    container: document.querySelector('.main'),
    loader: document.querySelector('.loader'),
    init: init
  };

  app.init();

  function init () {
    
  }
})();
